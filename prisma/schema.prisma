generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql" 
  url      = env("DATABASE_URL")
}

model board {
  id          String @id @db.VarChar
  title       String @db.VarChar
  description String
}
